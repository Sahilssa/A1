# Additional dependencies for the AI-agent video pipeline.
# Install alongside the tracker's existing requirements.txt:
#   pip install -r requirements.txt -r requirements_agents.txt

google-genai           # Gemini API (free tier) — script agent
edge-tts                # Microsoft Edge free TTS voices — voice agent
moviepy>=2.0            # video assembly — editor agent
Pillow                  # caption/title card rendering — editor agent
requests                # visual fetching (Pollinations / Pexels) — visual agent
google-api-python-client  # YouTube upload — publish agent
google-auth-oauthlib       # YouTube OAuth2 — publish agent
